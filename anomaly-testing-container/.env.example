# ============================================================================
# Configuración de Variables de Entorno
# ============================================================================
# Copia este archivo como .env y completa con tus datos reales

# SERVIDORES POSTGRESQL (separados por comas para ejecutar en múltiples)
POSTGRES_SERVERS=server1.postgres.database.azure.com,server2.postgres.database.azure.com

# CREDENCIALES DE POSTGRESQL
POSTGRES_USER=your_username
POSTGRES_PASSWORD=your_password
POSTGRES_DATABASE=adventureworks
POSTGRES_PORT=5432

# CONFIGURACIÓN DE TESTS
# Tiempo de espera entre tests (en segundos) para permitir ingesta en Fabric
DELAY_BETWEEN_TESTS=120

# Habilitar test de brute force (true/false)
ENABLE_BRUTE_FORCE=false

# Número de intentos de brute force
BRUTE_FORCE_ATTEMPTS=20

# ============================================================================
# >>> NUEVO v2.0: Configuración de Tráfico de Fondo Normal
# ============================================================================

# Habilitar tráfico de fondo normal (true/false)
# Simula actividad normal de base de datos durante la demo
ENABLE_BACKGROUND_TRAFFIC=true

# Intensidad del tráfico de fondo (low/medium/high)
# - low:    2-3 SELECTs/min, 0-1 UPDATE/min (aplicación poco usada)
# - medium: 5-8 SELECTs/min, 1-2 UPDATEs/min (aplicación normal) [RECOMENDADO]
# - high:   10-15 SELECTs/min, 3-5 UPDATEs/min (aplicación con carga)
BACKGROUND_TRAFFIC_INTENSITY=medium

# Duración del baseline inicial antes de introducir anomalías (en segundos)
# Recomendado: 180s (3 min) para establecer patrón normal en Fabric
BASELINE_DURATION=180

# Tiempo entre anomalías (en segundos) para volver a normalidad
# Recomendado: 300s (5 min) para mostrar contraste normal vs anómalo
ANOMALY_SPACING=300
